# Refine bakery section headings

## Design direction

Use a quiet, editorial bakery divider: cocoa Playwrite titles on the cream/patterned section surfaces, flanked by the project’s existing gold starburst ornaments. This replaces the generic full-width dark glass bars without adding a second visual language.

**Tokens:** cocoa `#3E2215` for title ink; gold `#E8C840` stays in the supplied flourishes; existing cream/patterned backgrounds remain unchanged; Playwrite AU SA remains the inherited typeface at its currently supported weight.

**Signature:** the existing gold flourishes become the clear shared motif across Recent Cakes, Live Offers, Brownies, Specials, Cakes, and Pastries.

## Changes

1. Update [index.html](C:/Users/sidda/Desktop/cakes/index.html) so the `Recent Cakes` `<h2>` uses the same desktop/mobile ornament markup as the other section headings:
   - Existing `headingLeft.png` / `headingRight.png` desktop assets.
   - Existing `MheadingLeft.png` / `MheadingRight.png` mobile assets.
   - Wrap label text in `.heading-text`.
   - Keep images decorative with `alt=""` and add `aria-hidden="true"`.

2. Refine the shared heading CSS in [css/menu.css](C:/Users/sidda/Desktop/cakes/css/menu.css):
   - Replace the full-width cocoa glass panel with a centered, max-width ornament-and-title row.
   - Remove the heading panel’s background, blur, border, radius, large horizontal padding, and heavy shadow.
   - Use the existing cocoa ink for the title, crisp text without text shadow, and a restrained responsive title size/spacing.
   - Let the left/right ornament images flex within bounded widths so short titles stay compact and long titles remain balanced.
   - Consolidate the <=992px, <=768px, and <=480px behavior: retain existing desktop/mobile asset swapping; progressively reduce ornament size and gaps; prevent wrapping, clipping, and horizontal overflow.

3. Clean up [css/recent-cakes.css](C:/Users/sidda/Desktop/cakes/css/recent-cakes.css):
   - Remove the special `.recent-cakes-heading` sizing rules, including the invalid inverted `clamp()` declarations and `!important`.
   - Let Recent Cakes inherit the shared heading system.
   - Leave the marquee, section background, animations, and thumbnails unchanged.

## Scope guardrails

- Do not change the Google Fonts URL, `font-family`, font weights, colors, content, card layout, navigation, backgrounds, or menu loading behavior.
- Do not add new assets, gradients, icons, or animations.
- Preserve existing semantic section `<h2>` elements and all non-heading CSS.

## Verification

- Review the diff to confirm changes are limited to heading markup and heading CSS.
- Check 1280px, 992px, 768px, 480px, and 320px layouts: ornaments are balanced, headings remain readable, and there is no horizontal overflow or clipping.
- Confirm all six section titles share the same treatment, with desktop ornaments above 768px and mobile ornaments at or below it.
- Verify existing marquee, offers, cake cards, navigation, and font rendering are unchanged.
