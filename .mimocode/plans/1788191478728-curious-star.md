# Restore a 300–700 font-weight hierarchy

## Goal

Update only font loading and `font-weight` declarations so the existing Playwrite AU SA styling, layout, colors, spacing, sizing, and behavior remain unchanged.

## Exact hierarchy

- General/body text: `300`
- Cake descriptions and Home/Offers/Menu/Contact navigation labels: `400`
- View Images, Show More, and cake prices: `500`
- Cake badges and cake names: `600`
- Section titles (Recent Cakes, Live Offers, Brownies, Specials, Cakes, Pastries): `700`

## Files and changes

1. In [index.html](C:/Users/sidda/Desktop/cakes/index.html), change only the Google Fonts weight range from `100..400` to `300..700`.
2. In [css/global.css](C:/Users/sidda/Desktop/cakes/css/global.css), keep the existing Playwrite family and set the base body weight to `300`; retain all non-typography rules unchanged.
3. In [css/menu.css](C:/Users/sidda/Desktop/cakes/css/menu.css), assign weights to the existing selectors only:
   - `.decorated-heading`: `700`
   - `.badges span` and `.top-row h2`: `600`
   - `.view-images-btn`, `.show-more-btn`, and `.info-row .price`: `500`
   - `.info-table`/description rows: `300`
4. In [css/home.css](C:/Users/sidda/Desktop/cakes/css/home.css), set `.aps-menu-label span` and its checked state to `400`.
5. In [css/offers.css](C:/Users/sidda/Desktop/cakes/css/offers.css), set offer card descriptions to `300` and preserve existing offer card title styling unless it is a section-title selector.
6. In [css/recent-cakes.css](C:/Users/sidda/Desktop/cakes/css/recent-cakes.css), preserve all existing rules; its section heading inherits the `700` `.decorated-heading` weight.

No selectors unrelated to font weight will be added or modified, and no HTML structure or JavaScript will change.

## Verification

- Inspect the diff to confirm only the font URL and `font-weight` declarations changed.
- Run JavaScript syntax checks to ensure the page scripts remain unaffected.
- Serve the site locally and verify all section headings, card labels, buttons, prices, descriptions, and navigation labels load with the intended hierarchy and no layout/color changes.
