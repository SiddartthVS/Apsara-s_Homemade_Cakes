# Fix Mobile View for Cake Website

## Plan

### 1. Improve card layout on mobile (`menu.css`)

Currently on mobile (≤768px): image=38%, details=62% side-by-side. This is too tight. Switch to a **stacked layout** (image on top, details below) for screens ≤480px, and keep the side-by-side but with better proportions for 481-768px.

**For ≤480px (small phones):**
- Card becomes `flex-direction: column`
- Image section: `width: 100%`
- Details section: `width: 100%`
- Image: reduce aspect-ratio or cap height

**For 481-768px (tablets/large phones):**
- Keep side-by-side but adjust: image=30%, details=70%
- Reduce padding/gaps further

### 2. Reduce badge and heading sizes for very small screens (`menu.css`)

Add a `≤480px` breakpoint to further reduce:
- Heading font size
- Badge padding
- Info row padding/font-size

### 3. Fix "Brownies" heading spacing (`menu.css`)

The decorated heading has `padding-top: 10px` which is fine, but ensure the section below it has proper margin.

## Files to Modify

- `menu.css` — Add `≤480px` breakpoint, adjust mobile card layout

## Verification

- Open the site on a mobile device or use Chrome DevTools responsive mode
- Verify cards stack vertically on small phones (≤480px)
- Verify side-by-side layout on larger phones (481-768px)
- Verify "View Images" button is fully visible and tappable
